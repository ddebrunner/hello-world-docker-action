name: M
on:
  workflow_dispatch:
jobs:
  mat:
     strategy:
       matrix:
         fruit: [apple, pear]
         animal: [cat]
         include:
           - color: pink
             animal: cat
             pink: dummy
           - color: blue
             animal: cat
             blue: dummy
           - fruit: banana
             animal: wolf
             color: teal
     runs-on: ubuntu-latest
     steps:
       - run: |
          echo fruit ${{ matrix.fruit }}
          echo animal ${{ matrix.animal }}
          echo color ${{ matrix.color }}
