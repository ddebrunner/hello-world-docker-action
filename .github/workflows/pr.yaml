name: TestCheck
on:
   pull_request:
     types: [opened, synchronize, reopened]

jobs:
  ddcheck:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v3
    - uses: stepzen-dev/stepzen-install@v1
    - uses: stepzen-dev/stepzen-login@700c413c3e42def627d5c36c0fc6c82fd2cf31b8
      with:
       domain: "us-east-a.ibm.stepzen.net"
       account: "danville"
       adminkey: ${{ secrets.SZDAN2 }}
