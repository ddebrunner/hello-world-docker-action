name: BR
on:
  workflow_call:
  workflow_dispatch:
jobs:
  bra:
     runs-on: ubuntu-latest
     steps:
         - uses: actions/setup-node@v4
           with:
             node-version: '22'
         - run: date
         - uses: stepzen-dev/stepzen-install@c7d7357bcd8a385b526a3476ef478a0c069776bb
         - run: date
         - run: npm exec --package=stepzen -c 'stepzen version'
  
