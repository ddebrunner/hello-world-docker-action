name: BR
on:
  workflow_call:
  workflow_dispatch:
jobs:
  bra:
     runs-on: ubuntu-latest
     steps:
         - uses: actions/setup-node@v4
           with:
             node-version: '22'
         - run: date
         - uses: stepzen-dev/stepzen-install@541119c61c4fde210f10482e55e41339e47d601a
         - run: date
         - run: npm exec --package=stepzen -c 'stepzen version'
  
